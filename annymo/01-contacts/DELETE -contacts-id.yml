info:
  name: DELETE /contacts/:id
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/contacts/{{contactId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
