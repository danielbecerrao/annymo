info:
  name: GET /contacts/:id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/contacts/{{contactId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
